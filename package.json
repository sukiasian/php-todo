{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
    	"serve": "concurrently \"php artisan serve\" \"cd frontend && npm run serve\""	
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^8.2.2",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
